# Дополнительные записи контрольного комплекта

После публикации этапа `5d00583` локальный сервер создал ещё два файла с ответами для загруженного контрольного комплекта. Они сохранены отдельно от синтетической оценки 23/24:

- `verify-changes-v2.aa08dbd78aeeb4ae.json` — первоначальная проверка сопоставлений;
- `semantic-lost-v2.9f88381bef2513c1.json` — смысловой поиск.

Проверено воспроизведение без API на файлах `data/polozhenie_red8_before.docx` и `data/polozhenie_red9_after.docx`. Для совпадения запроса нужны исходные имена загрузки:

```text
Положение_о_внутреннем_аудите_редакция_8_обезличено.docx
Положение_о_внутреннем_аудите_редакция_9_обезличено.docx
```

В воспроизведённом отчёте оба шага имеют `source: fixture`, режим — `demo`. Первоначальная проверка приняла 38 из 38 строк по меткам и ссылкам; смысловая — 7 из 7, предложила 5 кандидатов и для 2 пунктов не нашла пары. Отклонённых ссылок — 0; аудит сверил 389 вхождений источников. Исходные 7 потенциальных утрат остаются в основной таблице.

Это результат воспроизведения и проверки источников, **не экспертное подтверждение пяти смысловых соответствий**. Время и расход первоначальных живых запросов здесь не устанавливаются. Кнопка демо использует другие отображаемые имена файлов, поэтому её запрос имеет иной ключ записи; для данного воспроизведения предусмотрен тест:

```sh
cd server
node --test test/organizer-replay.test.js
```

Тест отключает API независимо от `.env`. Синтетическая оценка и её неизменные ожидания описаны в [semantic-review.md](semantic-review.md).
