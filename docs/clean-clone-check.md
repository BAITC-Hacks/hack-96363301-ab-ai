# Проверка свежего клона — 23 сентября 2026

Проверен опубликованный коммит `5d005832031e307ebbebeb95eaad930ada5d5f47` из командного репозитория `BAITC-Hacks/hack-96363301-ab-ai`. Использован новый каталог во временной папке Windows, без копирования `node_modules` и локального `.env` рабочего проекта. Среда: Windows, Node.js 22.23.1.

1. `git clone --depth 1 https://github.com/BAITC-Hacks/hack-96363301-ab-ai.git <новый каталог>` — получен указанный коммит.
2. В каталоге `server`: `npm ci --no-audit --no-fund` — установлены 187 пакетов по lock-файлу.
3. Отсутствие `server/.env` подтверждено. На время проверки `OPENAI_API_KEY` задан пробельным значением, чтобы исключить обращения к API из окружения.
4. `npm test -- --test-reporter=dot` — **165/165 тестов прошли**.
5. Приложение запущено на отдельном порту localhost: главная страница ответила HTTP 200 и содержит ссылку на собранные ресурсы фронтенда; `/api/health` вернул `mode: demo`.
6. `POST /api/analyze/semantic` — отчёт со смысловой проверкой `source: fixture`, проверено 8 исходных функций; исходные 8 кандидатов на утрату остались в основном сопоставлении.
7. `git status --short` после проверок пустой: установка и воспроизведение не изменили отслеживаемые файлы.

Сборка фронтенда в свежем клоне не требовалась: `web/dist` включён в репозиторий. Сборка, линтер, 21/21 контрольных проверок организатора и браузерная проверка компьютера/телефона выполнены в рабочем каталоге того же коммита — см. [аудит задачи](task-audit.md).

Эта проверка подтверждает запуск опубликованных материалов без ключа, но не статус сдачи на платформе организаторов. Качество смысловой классификации измеряется отдельно: зафиксированы 23/24 совпадения и один известный пропуск, а не 100% точности — [протокол оценки](semantic-review.md).
