# Проверка основной задачи — 23 сентября 2026

Основание: исходное задание `data/case-tz.pdf` (3 страницы), его текст в `CASE.md`, работающий код и проверки. Это оценка покрытия прототипа, а не выставленные жюри баллы.

После интеграции поиска переформулировок пройдены **165/165 тестов**, **21/21 контрольных проверок организатора**, сборка и линтер фронтенда. Внешний API при этих проверках отключён. Тесты охватывают ограничения поиска, подмену цитат и владельцев, динамическую схему ответа, воспроизведение трёх записанных комплектов и HTTP/Excel-сценарий. Для HTTP-тестов устранён случайный выбор запрещённых Fetch и зарезервированных Windows портов. До этого этапа проходили 133 теста.

Опубликованный коммит `5d00583` отдельно проверен в свежем клоне из GitHub: установка по lock-файлу, **165/165 тестов**, выдача собранного интерфейса и воспроизведение смыслового примера без `.env` и API-ключа. [Протокол чистого запуска](clean-clone-check.md).

Отдельно ранее выполнен реальный `npm run check:openai`: источник `api`, модель ответа `gpt-4.1-mini-2025-04-14`, 54 входных и 5 выходных токенов.

Первый живой `/api/analyze/example` на синтетическом учебном комплекте подтвердил обработку двух соответствий: принято 2 из 2, возвращены 3 ссылки, отклонено 0, время модельного шага — 3231 мс, режим отчёта — `live`; аудит проверил 39 вхождений ссылок. Для того же запроса проверялось воспроизведение без ключа: `source: fixture`, принято 2 соответствия, отклонённых ссылок — 0. Это историческая проверка интеграции и источников на одном примере; исходная версия ответа зафиксирована в коммите `13a8179`. [История живого прогона и сохранённого ответа](live-api-check.md).

Оценка нового поиска переформулировок отделена от проверки подключения. Финальный зафиксированный живой прогон: **23/24 совпадения ожидаемых статусов и адресов** на `gpt-4.1`; предшествующую проверку найденных сопоставлений выполняет `gpt-4.1-mini`. По наборам: `semantic-example` — 8/8, `semantic-holdout` — 7/8, `semantic-blind` — 8/8. Все 24 исходных пункта получили принятый результат; ошибочных предложений эквивалентности по разметке — 0, отклонённых ссылок — 0. Единственное расхождение: пара о доставке материалов ожидается как `candidate`, но получен `not_found`. Ожидание не изменяли. Порог полного совпадения 24/24 не пройден, поэтому оценочная команда завершается кодом 1.

Ожидания первых двух наборов записаны до первого живого вызова. После обратной связи они стали регрессионными. `semantic-blind` подготовлен отдельно, но участвовал в сравнении и выборе моделей, поэтому его результат не является полностью независимой финальной оценкой. Сохранены все измерения: [версия 1 — 6/16](semantic-evaluation-v1.json), [версия 2 на mini — 16/24](semantic-evaluation-mini-v2.json), [финальный прогон — 23/24](semantic-evaluation.json). Протокол и ограничения: [semantic-review.md](semantic-review.md). Эти результаты относятся к малому синтетическому набору, а не к неизвестным документам компаний.

Полный браузерный сценарий пройден на отдельном сервере без ключа: карта, решения и их восстановление, контрпроверка, экспорт, загрузка 2+2 файлов, удаление и повторное добавление, паспорт и имена в цитатах. Дополнительно проверены смысловые пары, фильтры, дословные фрагменты, скачивание примеров и экран шириной 390 px. Исправлено горизонтальное переполнение старых таблиц на телефоне. Ошибок JavaScript нет. Счётчики сверены с ответом API; снимки `13-semantic-review.png` и `14-semantic-mobile.png` созданы и просмотрены. Снимок `12-document-collection.png` относится к предыдущему этапу.

## Что готово

Основной путь реализован: загрузка двух комплектов → определение подразделений → сопоставление функций между комплектами → находки с цитатами → заключение. На каждую редакцию принимаются 1–5 файлов DOCX, текстового PDF или XLSX; форматы можно смешивать. Лимит файла — 20 MiB, суммарно двух редакций — 100 MiB (1 MiB = 1 048 576 байт; в интерфейсе обозначено как МБ). Контрольный комплект организатора и независимые учебные комплекты доступны без API-ключа.

| Обязательное требование | Реализация и доказательство | Граница применения |
|---|---|---|
| Реорганизованные, сохранённые и созданные подразделения | `analysis/units.js`; тесты контрольного состава, переименования, слияния и разделения | Нужны распознаваемые названия и явные основания преобразования; сходство названий не доказывает преемственность |
| Сопоставление функций и потенциальная утрата | `analysis/diff.js`; известные передачи организатора и независимый пример утраты. `llm/semantic.js` отдельно ищет возможные переформулировки, в том числе без общих значимых слов | Модельный поиск ограничен по объёму и предлагает пары для эксперта. Основные статусы и счётчики не меняются. Утрата всегда потенциальная |
| Дублирование и конфликт интересов | Прямые пары пересечений, проверка совмещения контроля качества и планирования | Детектор конфликтов реализует конкретное правило, а не универсальную проверку разделения полномочий |
| Источник каждого существенного вывода | Цитаты парсера с именами файлов; `analysis/evidence-audit.js` проверяет весь выдаваемый отчёт, обе редакции, принадлежность файлам и фрагменты контрпроверки | Совпадение адреса и текста не доказывает правильность интерпретации |
| Понятное итоговое заключение | `llm/enrich.js`, `ConclusionSection.tsx`; наблюдения, рекомендации и источники | Заключение формируется из найденного. При неопределённых владельцах явно сообщает о неполном анализе |

Обязательные артефакты есть: исходники, интерфейс загрузки и просмотра, собранный фронтенд, README с запуском и архитектурой, контрольные данные и воспроизводимые проверки.

## Что исправлено при этой проверке

- После проверки найденных сопоставлений добавлен самостоятельный модельный поиск для функций без текстовой пары. Рассматриваются пункты новой редакции той же области функций, без отсечения по сходству слов. Модель может предложить пару или изменение смысла; локальная проверка отклоняет чужие адреса и недословные фрагменты. Непроверенный пункт остаётся явно непроверенным.
- Интерфейс «Поиск переформулировок» показывает цитаты рядом, владельцев, фактический источник ответа, проверенное число исходных функций и охват новой редакции. Новая кнопка запускает синтетический пример; исходные XLSX доступны для скачивания. Предложения экспортируются на отдельный лист «Смысловая проверка» и не переклассифицируют функции, находки плана или счётчики.
- Сохранение модельного ответа фиксирует первый файл для ключа запроса без перезаписи последующими запусками. Первичные и доработанные измерения этого этапа сохранены в отдельных JSON; версии и метрики описаны в протоколе.
- Комплекты поддерживают несколько файлов на сторону. Интерфейс дополняет выбор, позволяет удалять отдельные файлы и сохраняет прежний выбор при невалидной подборке. API принимает повторяющиеся multipart-поля `before` и `after`; лимиты проверяются на сервере.
- Файлы разбираются отдельно: совпадающие номера пунктов в разных приложениях не смешиваются. В многодокументной редакции адрес содержит идентификатор файла на основе хеша его байтов; перестановка неизменённых файлов не меняет ссылки. Для одного файла сохранён прежний формат адресов. Побайтовая копия в той же редакции отклоняется с объяснением; одинаковый файл в двух редакциях допустим.
- Определения подразделений из файлов объединяются внутри редакции; противоречивые названия и аббревиатуры отклоняются. Явно названный владелец разрешается по составу всей редакции, общий заголовок действует только в пределах состава своего файла. Сопоставление функций и поиск пересечений выполняются по всему комплекту.
- Короткая подпись подразделения в XLSX, например «ОК», связывается с определением «Отдел контроля (ОК)» из комплекта с сохранением источников обоих определений. Явная аббревиатура в заголовке имеет приоритет перед совпадениями отдельных слов названия.
- Состав файлов виден в отчёте и на листе Excel «Комплект документов». Цитаты, источники решений и контрпроверка показывают имя исходного файла. Паспорт содержит отдельные показатели каждого файла. [Снимок четырёх файлов](screenshots/12-document-collection.png).
- Повторные числовые номера пунктов больше не перезаписывают источники и владельцев: неоднозначный документ отклоняется с объяснением. Повторные буквенные списки сохраняют отдельные адреса; явное оглавление не подменяет разделы.
- Продолжение буквенного подпункта прикрепляется к его цитате.
- Короткие обязанности вроде «Согласует договоры» не исчезают из-за порога длины.
- Заголовок «Руководитель службы поддержки» распознаётся без обязательной аббревиатуры.
- Обязанности явно названной роли без отдельного подразделения включаются в сравнение под названием роли. Это вернуло в анализ 20 пунктов 5.3 редакции 8; подразделения из ролей не создаются. Три новых кандидата на утрату разобраны в `docs/loss-review.md`.
- Строки XLSX без определённого подразделения сохраняются как источники предупреждений, а не исчезают.
- Паспорт «Что вошло в анализ» показывает распознанные пункты, владельцев, пропуски и общие назначения. Полнота не выражается вымышленным процентом.
- Проверка цитат выполняется перед каждым ответом API, а не только в тестах. Паспорт и его источники также попадают в Excel.

## Что ещё остаётся

**Для более широкого применения:** OCR и оргсхемы; оценка смыслового поиска на неизвестных корпоративных документах; дополнительные правила конфликта интересов. Комплекты ограничены 5 файлами на сторону и распознаваемой структурой. Нормативные пробелы определяются по ограниченным признакам упоминания подразделений и описания функций, без юридической проверки. Общие заголовки даже в пределах своего файла могут требовать более узкой интерпретации. Нераспознанные конструкции могут остаться вне диагностики даже при отсутствии предупреждений.

**Стабильность источников:** идентификатор файла зависит от его байтов, поэтому повторное сохранение DOCX/XLSX может изменить его даже при неизменном видимом тексте. Гарантируется независимость ссылок от порядка одинакового набора неизменённых файлов; добавление второго файла переводит адреса из совместимого одиночного формата в формат комплекта. Отсутствие распознанных функций в отдельном приложении отражается в паспорте, но не доказывает отсутствие обязанностей в его содержимом.

**Границы модельного поиска:** до 12 функций без пары, до 80 пунктов «после» в соответствующих областях функций, до 64 000 символов входного JSON и до 4096 выходных токенов. Охват показывается отдельно; отсутствие пары в ограниченном наборе не доказывает утрату. Смысловые предложения не изменяют основное сопоставление и требуют решения эксперта. Малые синтетические наборы и случаи, уже использованные для настройки, не дают общей оценки точности на неизвестных документах.

**Границы подтверждения:** первичная проверка API, регрессионные тесты и смысловая оценка — разные доказательства. В первом учебном ответе, зафиксированном в `13a8179`, модель написала «была удалена», но `applyModelExplanations` не публикует свободный `rationale`: формируется локальное пояснение о ненайденном соответствии и необходимости проверки. Точное совпадение цитат и структурная валидация не доказывают правильность смыслового вывода.

**Опциональные пункты задания:** законодательство/стандарты и сравнение с другими операторами не реализованы. Предложение перераспределения функций реализовано как рабочий план пользователя с обоснованием, источниками и проектом формулировки.

**Сдача:** коммит `5d00583` отправлен в `main` командного GitHub и проверен свежим клонированием. Статус отправки решения на платформе организаторов не подтверждён; push его не заменяет.

## Как воспроизвести

1. `cd server` и `npm test` — независимые примеры, форматы и комплекты, регрессии, API, проверка подмены цитат, план и Excel. Тесты комплектов включают совпадающие номера в разных файлах, перестановку, повторные копии и ограничения загрузки.
2. `npm start` — оставить сервер в этом терминале, открыть `http://localhost:3000`, проанализировать комплект организатора и раскрыть «Что вошло в анализ».
3. В другом терминале из `server`: `npm run verify` — 21 контрольная проверка документов организатора, включая точное совпадение цитат.
4. «Проверить учебный пример» — переименование, передача, утрата и пересечение.
5. «Найти скрытые изменения» — четыре изменения формулировок, рассмотрение с источниками и экспорт.
6. Добавить несколько файлов в каждую редакцию и запустить анализ загруженных документов. Проверить состав отчёта, имена рядом с цитатами, отдельные строки паспорта и лист «Комплект документов» в Excel.
7. При запущенном сервере и отдельном тестовом Chrome на порту 9333 открыть другой терминал в `server` и выполнить `node scripts/browser-smoke.js`. Сценарий включает загрузку XLSX со строкой без владельца, четыре файла с совпадающими адресами строк, имена источников, решения и экспорт. Флаг `--screenshots` сохраняет снимки сценария.
8. «Найти переформулировки» — синтетический пример дополнительного поиска. Проверить источник ответа, охват, предложенные пары, непроверенные пункты и сохранение основных статусов. Исходники: `/api/examples/semantic/before.xlsx` и `/api/examples/semantic/after.xlsx`.
9. Из `server`: `npm run eval:semantic` — оценка сохранённых ответов без API, даже при ключе в окружении. Для нового живого измерения нужен явный `--live`: до двух модельных вызовов на комплект, до шести для трёх текущих наборов. `--output <путь>` сохраняет результаты каждого случая и трассировку. При зафиксированных 23/24 команда возвращает 1 из-за одного расхождения; это сигнал о неполном прохождении критерия оценки.
