---
description: Финальная сборка — проверка с нуля, README, демо-сценарий, текст защиты
argument-hint: [осталось времени]
---

Осталось до дедлайна: $ARGUMENTS

Финал. Отвечай по-русски. Приоритет: чтобы проект **запустился у чужого человека** и чтобы демо прошло без сюрпризов.

Выполняй параллельно:

- Запусти `hack-pitch` — README, сценарий демо и текст защиты по фактическому коду.
- Запусти `hack-reviewer` — последний прогон демо-пути на критичные баги и секреты.

Сам одновременно сделай:

1. Проверку запуска с чистого состояния: по шагам из README, как будто ты впервые видишь проект. Всё, что не заработало — записывай.
2. **Запуск БЕЗ API-ключа — обязательная проверка.** Убери `OPENAI_API_KEY` из окружения и пройди демо-сценарий целиком. Отбор в финал идёт заочно 24–28 сентября, у эксперта нашего ключа не будет: если без него приложение падает или показывает пустой экран, проект не оценят вовсе. Не проходит — это блокер уровня КРИТИЧНО, чини раньше всего остального.
3. `.env.example` со всеми нужными переменными без значений. Убедись, что `.env` в `.gitignore` и не в истории.
4. Прогон демо-сценария целиком на тех самых данных, которые будешь показывать — и с ключом, и без.
5. **Доказательства в репозитории:** положи скриншоты или короткую GIF прохода по сценарию. Если у эксперта что-то не заведётся на его машине, он должен увидеть, что у нас работало.
6. Сверку README с **официальной структурой организаторов** — 11 обязательных пунктов: название, описание проблемы, что реализовано, как работает сценарий, технологии, архитектура, установка и запуск, как жюри проверит решение, данные и интеграции, ограничения, ссылка на развёрнутую версию. Отсутствие пункта — потерянные баллы на ровном месте.
7. **Таблицу трассировки требований** в разделе «как проверить решение»: требование кейса дословно → файл → команда проверки. Эксперт должен ставить галочки, а не искать. Невыполненные требования — честно в «Ограничения».
8. **Раскрытие в README — обязательное требование регламента.** Раздел должен перечислять:
   - заранее подготовленные инструкции помощников (`.claude/agents/`, `.codex/agents/`, `HACKATHON-ASSISTANTS.md`, подготовлены 20.09.2026, реализации кейса не содержат);
   - **использованный шаблон проекта**, если старт был со скелета из `C:\Users\huawei\scaffolds` — назови какой (`nest-api`, `vite-react-ts`, `flutter-app`, `express-api`) и укажи, что это boilerplate генератора с установленными зависимостями, без логики кейса;
   - использованные AI-инструменты и агенты, библиотеки, модели и датасеты.

   Проверь, что ничего существенного не пропущено — «проект преимущественно создан третьими лицами без раскрытия этого факта» означает дисквалификацию, а раскрытый факт использования AI полностью разрешён правилом 03.
9. Проверку перед публикацией: в репозитории нет ключей, токенов и персональных данных ни в рабочем дереве, ни в истории. Репозиторий остаётся приватным — доступ только у команды и организаторов.
10. Финальный commit и push, с запасом до 18:00. Не оставляй push на последнюю минуту: в последний час выходить с площадки нельзя, и чинить что-то в 17:58 будет негде.
11. **Сдачу на платформе — последний и обязательный шаг.** Открой страницу треков, нажми «Сдать решение» по своему кейсу, заполни название и описание проекта. Без этого работа формально не сдана, сколько бы ни было в GitHub. Если сдавали черновиком раньше — обнови до финальной версии и подтверди мне, что обновление прошло.

В конце дай мне:

```
ЗАПУСКАЕТСЯ С НУЛЯ: да/нет + что мешает
РАБОТАЕТ БЕЗ API-КЛЮЧА: да/нет ← если нет, это блокер отбора
ДЕМО ПРОЙДЕНО: да/нет
README ПО СТРУКТУРЕ ОРГАНИЗАТОРОВ: <каких пунктов не хватает>
ТРАССИРОВКА ТРЕБОВАНИЙ: <закрыто N из M>
СДАНО НА ПЛАТФОРМЕ: да/нет + время
ТЕКСТ ЗАЩИТЫ: <...>
ЧТО ГОВОРИТЬ ПРО НЕДОДЕЛКИ: <честная формулировка>
ОСТАЛОСЬ НЕСДЕЛАННЫМ: <...>
```

После мероприятия напомни мне удалить временные ключи и токены, выданные организаторами, и локальные копии предоставленных данных — этого требует инструкция по кибербезопасности.
