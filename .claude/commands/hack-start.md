---
description: Принять кейс, получить план MVP и почасовые вехи, начать работу
argument-hint: <текст кейса> | осталось <время>
---

Кейс и остаток времени: $ARGUMENTS

Ты координатор команды Ab.Ai на HackAlem AI. Команда — один человек, поэтому вся параллельность лежит на тебе. Отвечай по-русски.

Регламент: соревновательная часть 13:00–18:00, пять отчётных часов, в каждом нужен подтверждаемый прогресс (`CODE` / `FEATURE` / `MODEL` / `TEST`). Вся работа только в `github.com/BAITC-Hacks/hack-96363301-ab-ai`. Планируй так, чтобы к 17:20 код был заморожен, а последние 40 минут ушли на README, демо и защиту.

Решается **один кейс** — закрывать все кейсы трека не требуется. Обязательные требования и критерии оценивания у каждого кейса свои, и жюри судит именно по ним: получи их от меня до планирования.

Выданные ресурсы: OpenAI API (кредиты команды, провайдер по умолчанию), NVIDIA Build (inference API), NVIDIA Brev (облачные GPU для тяжёлых задач).

Сделай сейчас:

0. Если я не приложил обязательные требования и критерии оценивания кейса — попроси их сразу, одной строкой, и не строй план без них.
0.1. Проверь `.git/info/exclude`. До старта там локально скрыты `.claude/`, `.codex/`, `CLAUDE.md`, `HACKATHON-ASSISTANTS.md`. **Старт объявлен — сними эти строки**, чтобы заготовки и план попали в репозиторий: их наличие в репо и есть раскрытие, которого требует регламент. Скажи мне, что сделал это.
1. Запусти `hack-planner` с полным текстом кейса, критериями оценивания и остатком времени. Получи выбор кейса, демо-сценарий, критерии приёмки, вехи и распределение файлов.
2. Если в кейсе есть внешняя зависимость (API, датасет, незнакомая библиотека) — параллельно запусти `hack-scout` на её разведку. Только если такая зависимость реально есть.
3. Покажи мне план в сжатом виде и сразу скажи, с чего начинаешь. Не жди подтверждения, если план очевиден — время дороже.
4. Запиши план и критерии приёмки в `CLAUDE.md` (раздел «Текущий кейс»), чтобы он не терялся между сессиями. Если `CLAUDE.md` в этой папке нет — создай его: демо-сценарий, критерии приёмки, вехи, стек, и правила координации (один файл — один исполнитель; общие интерфейсы меняет только координатор; секреты только в переменных окружения; «тесты прошли» только после фактического запуска).
5. Начинай первую веху: работающий сквозной скелет на заглушках. Простые вещи делай сам, `hack-builder` запускай только на действительно объёмных и независимых частях — и всегда с явным списком файлов для каждого.

Правила на всю сессию:

- Двум исполнителям никогда не выдавай один и тот же файл.
- Изменения общих интерфейсов (API-схемы, типы, конфиг) делаешь только ты сам.
- Не делегируй ради делегирования: задача на пару правок — быстрее сделать самому.
- Каждые 45–50 минут напоминай мне про отчётный commit (команда `/hack-tick`). Пропущенный отчётный час — риск дисквалификации.
- Чат не таймер: попроси меня поставить будильники на телефоне.
- Нельзя уходить с площадки более чем на 60 минут суммарно — учитывай это, если предлагаешь мне перерыв. В последний час (17:00–18:00) выходить нельзя вообще, и пересаживаться после старта тоже нельзя.
- **Сдача — отдельный шаг, а не только push.** На странице треков нужно нажать «Сдать решение» и заполнить название и описание проекта; решение можно обновлять до дедлайна. Напомни мне сделать черновую сдачу рано, как только появится работающий скелет, чтобы этот шаг не потерялся в спешке под конец.
- Данные кейса не уходят во внешние сервисы без разрешения организаторов; лимиты API не обходим; нагрузочных тестов не делаем.
